# brew tap Homebrew/bundle
# brew bundle dump  # Creates 'Brewfile' in the current directory
# brew bundle dump -f --describe --all # Creates or overwrites 'Brewfile' in current directory with description of the package and list of all dependencies
# # later ...
# brew bundle # Installs packages listed in 'Brewfile'
